interface:
  display_name: "Image to Editable PPT"
  short_description: "Convert slide images/PDF/image-based PPTX into high-fidelity editable PowerPoint"
  default_prompt: "使用 $image-to-editable-ppt 将输入重建成高保真可编辑 PowerPoint。Run runtime doctor first, then prepare the deck run, dispatch each page to a page subagent, use $imagegen / GPT Image 2 for clean backgrounds and complex visual assets, keep main text as native editable text, preserve speaker notes, and complete manifest, preview, contact sheet, qa_review, validation, and final PPTX checks."
